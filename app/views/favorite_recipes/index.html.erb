<div class="container">
  <h1>My Recipes</h1>
  <% @favorite_recipes.each do |favorite| %>
    <div class="white-container d-flex justify-content-between">
     <div><h3><%= favorite.recipe.title %></h3></div>
      <div>
        <%= link_to "Remove", favorite_recipes_path, method: :delete, class: "btn btn-light" %>
        <%#= link_to "Show", food_recipe_path(favorite.recipe.foods.first, favorite.recipe), class: "btn btn-light" %>
      </div>
    </div>
  <% end %>

</div>
